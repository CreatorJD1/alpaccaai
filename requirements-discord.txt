# Optional Discord bridge with encrypted voice output and bounded local receive.
#   python -m pip install -r requirements-discord.txt
discord.py[voice]>=2.7.1
davey>=0.1.6
imageio-ffmpeg>=0.6.0
# Alpha upstream API: pin the reviewed receive implementation exactly.
# Inbound DAVE compatibility stays repo-local until upstream PR #58 ships.
discord-ext-voice-recv==0.5.2a179
faster-whisper>=1.2.1
# CPU-only speech onset/endpoint detection. silero-vad supplies the official
# ONNX weights; Alpecca loads them directly so PyTorch is not imported.
onnxruntime>=1.16.1
silero-vad>=6.2.1
